# Модульное рабочее пространство (VERM)

Единое модульное рабочее пространство на Python с графическим интерфейсом, предназначенное для работы с данными маркетплейсов **Wildberries** и **Ozon**: выгрузка карточек товаров, характеристик и ведение локальной базы данных. Для Wildberries реализована полная выгрузка каталога и характеристик; для Ozon — выгрузка товаров (актив/архив) и справочника атрибутов.

---

## 🚀 О проекте

Проект представляет собой **модульный лаунчер** с графическим интерфейсом на базе [CustomTkinter](https://github.com/TomSchimansky/CustomTkinter). При запуске приложение автоматически сканирует папку `apps/`, находит все модули (Python-скрипты) и строит под них интерфейс в виде кнопок. Каждый модуль можно запустить одним кликом — выполнение происходит в отдельном фоновом потоке, поэтому интерфейс не «зависает» во время длительных операций (например, выгрузки данных из API).

### Ключевые архитектурные особенности

| Особенность | Описание |
| --- | --- |
| 🧩 **Модульность** | Любая новая функция добавляется как отдельный скрипт в `apps/` — лаунчер подхватывает её автоматически, без правки самого лаунчера. |
| 🐍 **Общее виртуальное окружение** | Все модули используют единый интерпретатор и зависимости из `.venv/` (см. `requirements.txt`). |
| 🗄️ **Единое хранилище данных** | Все данные, логи и служебные файлы хранятся в одном месте — каталоге `data/`. |
| 🧵 **Фоновые потоки** | Модули запускаются в отдельных потоках; обновление GUI безопасно выполняется только из главного потока через очередь сообщений. |
| 🔐 **Секреты в `.env`** | 8 токенов Wildberries (1 мастер + 7 специализированных), мастер-токен Ozon (`OZ_MASTER_TOKEN`), а также параметры складов (`WB_WAREHOUSE_ID`, `OZON_WAREHOUSE_ID`) и `OZON_CLIENT_ID` хранятся в корневом файле `.env`, который не попадает в Git. |
| ⏱️ **Rate Limiter** | Встроенный Token Bucket (`WBRateLimiter`) ограничивает частоту запросов по категориям API и защищает от HTTP 429. |

---

## 💻 Текущий функционал системы

### Ядро (лаунчер) — `main.py`

Лаунчер отвечает за **обнаружение и запуск модулей**, а также за построение интерфейса.

**Возможности ядра:**

- **Динамическое сканирование модулей.** Функция `discover_modules()` просматривает каталог `apps/` на наличие файлов `*.py`, игнорируя `__init__.py` и служебные файлы, чьи имена начинаются с `_`. Результат сортируется по алфавиту.
- **Автоматическое построение интерфейса.** Для каждого найденного модуля создаётся кнопка в прокручиваемом списке; подпись кнопки формируется из имени файла (символы `_` заменяются на пробел, применяется заглавный регистр). Кнопка «Обновить список» позволяет пересканировать каталог без перезапуска.
- **Запуск скриптов в фоновых потоках.** При нажатии кнопка блокируется (переходит в состояние «выполняется…»), а модуль запускается в отдельном `threading.Thread`. Модуль должен содержать вызываемую функцию `run()`.
- **Потокобезопасное обновление GUI.** Так как Tkinter/CustomTkinter не потокобезопасны, рабочий поток передаёт результат в главный поток через `queue.Queue`; интерфейс опрашивает очередь каждые 100 мс (`_poll_queue`). Это исключает зависание и «креш» интерфейса.
- **Повторный запуск с перезагрузкой.** Если модуль уже импортирован, при повторном запуске он перезагружается через `importlib.reload()`, чтобы подхватывались свежие правки кода.
- **Гибкая настройка.** Параметры внешнего вида (тема оформления, цветовая схема, размер окна) читаются из `data/config.json` и имеют значения по умолчанию. Переключатель «Тёмная тема» меняет тему на лету.
- **Автозапуск, таймер и полная сверка DBase.** Если в конфиге включён параметр `dbase_auto_start`, при старте лаунчер автоматически сбрасывает курсор и запускает **полную** выгрузку `DBase.py` (со сверкой удалённых карточек), поскольку за время простоя на площадке могли удалить карточки. Далее модуль циклично перезапускается в инкрементальном режиме через `root.after()` с интервалом `dbase_interval_minutes`; если модуль в этот момент ещё выполняется, таймер пропускает цикл. Дополнительно таймер проверяет метку времени последнего обновления (`data/dbase_last_update.json`) и пропускает запуск, если `DBase` уже обновлялся меньше заданного интервала назад (например, сразу после создания карточки в Cards Creator) — чтобы не делать лишнюю выгрузку.
- **Ежедневная полная сверка.** Раз в сутки (в 8:00 по локальному времени) лаунчер автоматически сбрасывает курсор и запускает полную выгрузку с пометкой удалённых карточек (константа `DAILY_FULL_SYNC_HOUR` в `main.py`), даже если программа работает без перезапуска.
- **Кнопка полного обновления БД.** Рядом с кнопкой «D Base» расположена квадратная кнопка «🔄» (две закруглённые стрелки — значок обновления). Она удаляет файл курсора `data/wb_cards_cursor.json` и запускает полную выгрузку каталога заново (служебный модуль `apps/_Full_Update.py`), предварительно запросив подтверждение.
- **Настройки таймера.** Рядом с кнопкой «D Base» (справа от «🔄») расположена кнопка «⚙️», открывающая модальное окно (`ctk.CTkToplevel`), где можно включить/выключить автозапуск и изменить интервал обновления в минутах. Сохранение перезаписывает `data/config.json` и сразу перенастраивает активный таймер под новый интервал.
- **Настройки токенов и параметров API.** В том же окне настроек есть прокручиваемые секции «Токены WB API» (8 полей), «Токены Ozon API» (поле `OZ_MASTER_TOKEN`) и «Параметры API» (`WB_WAREHOUSE_ID`, `OZON_WAREHOUSE_ID`, `OZON_CLIENT_ID`). При сохранении значения перезаписываются в файл `.env`, а пустые специализированные токены WB автоматически откатываются на мастер-токен через `get_wb_token(category)`.

### 🔐 Управление токенами и параметрами (Wildberries и Ozon)

Проект хранит токены и параметры API в корневом файле `.env`, а единая логика чтения реализована в `apps/DBase.py`.

**Wildberries** — гибкое распределение лимитов запросов по категориям методов:

- **`get_wb_token(category)`** — возвращает токен для категории (`CONTENT`, `PRICES`, `STOCKS`, `MARKETPLACE`, `STATISTICS`, `PROMOTION`, `FEEDBACKS`). Если специализированный токен не заполнен, автоматически возвращается мастер-токен.
- **Мастер-токен (`WB_MASTER_TOKEN`)** — резервный токен для всех категорий. Для обратной совместимости старый ключ `WB_API_KEY` также воспринимается как мастер-токен.
- **8 полей токенов:** `WB_MASTER_TOKEN`, `WB_TOKEN_CONTENT`, `WB_TOKEN_PRICES`, `WB_TOKEN_STOCKS`, `WB_TOKEN_MARKETPLACE`, `WB_TOKEN_STATISTICS`, `WB_TOKEN_PROMOTION`, `WB_TOKEN_FEEDBACKS`.

**Ozon** — на начальном этапе интеграции:

- **`get_oz_token(category)`** — возвращает токен Ozon для категории методов (пока доступна только `MASTER`).
- **`OZ_MASTER_TOKEN`** — мастер-токен Ozon, единственное поле Ozon на данный момент (остальные токены будут разделены позже, когда станет понятно, какие из них нужны).

**Параметры API (не токены):**

- **`WB_WAREHOUSE_ID`** — массив ID складов Wildberries. Формат: JSON-массив (например `"[37356, 12345]"`) или перечисление через запятую. Читается через `get_wb_warehouse_ids()`.
- **`OZON_WAREHOUSE_ID`** — массив ID складов Ozon (аналогичный формат). Читается через `get_oz_warehouse_ids()`.
- **`OZON_CLIENT_ID`** — Client-Id продавца Ozon. Читается через `get_oz_client_id()`.

**Графическое управление.** В окне настроек лаунчера (кнопка «⚙️») доступны прокручиваемые секции «Токены WB API» (8 полей), «Токены Ozon API» (поле `OZ_MASTER_TOKEN`) и «Параметры API» (`WB_WAREHOUSE_ID`, `OZON_WAREHOUSE_ID`, `OZON_CLIENT_ID`); все значения можно отредактировать и сохранить обратно в `.env` (вставка значения — через `Ctrl+V` / `Ctrl+М` в русской раскладке).

### ⏱️ Rate Limiter (Token Bucket)

Для защиты от ошибок HTTP 429 (`Too Many Requests`) в `apps/DBase.py` реализован встроенный многопоточный ограничитель запросов **`WBRateLimiter`** на базе алгоритма **Token Bucket** («маркерная корзина»).

**Принцип работы:**

1. Для каждой категории API (`CONTENT`, `PRICES`, `STOCKS`, `MARKETPLACE`, `STATISTICS`, `PROMOTION`, `FEEDBACKS`) создаётся независимая «корзина» токенов.
2. Каждая корзина описывается двумя параметрами:
   - **`max_tokens`** — ёмкость корзины (допустимый всплеск/burst запросов);
   - **`refill_rate`** — скорость пополнения (сколько токенов добавляется в секунду).
3. Перед каждым HTTP-запросом к Wildberries рабочий поток вызывает **`limiter.wait_for_token(category)`**:
   - если в корзине есть токен — он забирается и запрос выполняется сразу;
   - если токенов нет — поток вычисляет время до появления следующего токена и блокируется через `time.sleep()`, мягко приостанавливая выгрузку до безопасного момента.
4. Все корзины потокобезопасны: каждая защищена `threading.Lock`, поэтому несколько фоновых потоков (модулей) не конфликтуют между собой.

**Лимиты по умолчанию (согласно документации Wildberries):**

| Категория | Лимит | `max_tokens` (burst) | `refill_rate` |
| --- | --- | --- | --- |
| `CONTENT` | 100 запросов/мин (~600 мс) | 5 | ~1.667 ток/сек |
| `PRICES` | 3 запроса/сек (~333 мс) | 3 | 3 ток/сек |
| `STOCKS` | 5 запросов/сек (~200 мс) | 5 | 5 ток/сек |
| `MARKETPLACE`, `STATISTICS`, `PROMOTION`, `FEEDBACKS` | базовый безопасный лимит | 2 | 2 ток/сек |

Глобальный экземпляр `LIMITER` доступен всем модулям через `apps.DBase`; модули `DBase.py` и `Stack.py` вызывают `wait_for_token("CONTENT")` перед каждым запросом к Content API вместо жёстких `time.sleep(0.6)`.

### Подключённые модули

Сейчас в каталоге `apps/` физически находятся **пять модулей** (плюс служебные):

#### 📦 `DBase.py` — база данных и интеграция с Wildberries Content API v2 и Ozon Seller API

Модуль `DBase` — **единственная точка управления схемой базы данных** проекта. Только ему разрешено создавать, изменять или удалять таблицы и колонки SQLite-базы `data/inventory.db`; все остальные модули могут лишь читать, добавлять или обновлять строки.

**Что делает модуль при запуске (монолитный алгоритм инициализации):**

**Wildberries:**

1. **Проверка токена.** Получает токен категории `CONTENT` через `get_wb_token("CONTENT")` из файла `.env`. Если файл отсутствует — создаёт его с шаблоном из полей WB и Ozon; если специализированный токен пуст, используется `WB_MASTER_TOKEN` (fallback), а при его отсутствии — завершает работу с сообщением об ошибке.
2. **Создание/обновление схемы БД.** Создаёт таблицы, при необходимости мигрирует структуру `wb_products` и добавляет колонки в `wb_charcs`, создаёт частичные уникальные индексы, наполняет справочник `wb_charcs` базовым маппингом (Seed) и таблицу `promts` базовыми промтами DeepSeek (Seed).
3. **Выгрузка карточек товаров.** Обращается к `POST /content/v2/get/cards/list`, с пагинацией курсором выкачивает все карточки и раскладывает их по таблицам:
   - корневые («паспортные») параметры → `wb_products`;
   - динамические характеристики (паттерн EAV) и вложенные объекты (`photos`, `documents`) → `wb_product_values`.
4. **Синхронизация справочника характеристик.** По уникальным категориям товаров опрашивает `GET /content/v2/object/charcs/{subjectId}` и заполняет таблицу `wb_charcs` шаблонами полей.

**Ozon (необязательно — пропускается, если не заполнены `OZ_MASTER_TOKEN` и `OZON_CLIENT_ID`):**

5. **Выгрузка товаров.** Обращается к `POST /v3/product/list` (пагинация `last_id`), затем к `POST /v3/product/info/list` батчами по `product_id` (активные → `oz_products`, архивные → `oz_archive`, вложенные поля → `oz_product_values`), а также к `POST /v4/product/info/attributes` — характеристики (`oz_product_values.attribute_id`) и габариты товаров.
6. **Синхронизация справочника атрибутов.** По уникальным категориям опрашивает `POST /v1/description-category/attribute` и заполняет таблицу `oz_charcs`.

**С какими данными работает модуль:**

| Таблица | Назначение |
| --- | --- |
| `brands` | Справочник брендов: юридическое название, названия на WB/Ozon, альтернативные имена для поиска (JSON-массив `search_aliases`), страна. Распознанное DeepSeek значение сопоставляется со справочником без учёта регистра и подставляется в карточку как точное `wb_name`/`oz_name`. |
| `status` | Составной ключ товара `UNIQUE(ART, SUP)` (артикул + буквенный суффикс поставщика). |
| `category_fields` | Выбранные пользователем поля (конструктор) для категорий: `subject_id` + JSON-список ключей полей (`charc:<charcID>` / `field:price` / `field:cost`), которые показываются и собираются при создании карточки этой категории. |
| `promts` | Промты для DeepSeek: `code` (уникальный ключ), `task_type` (`extract` — точное извлечение характеристик, `description` — творческое описание), `variant` (`standard`/`kit`/`foreign`/`generator`/`custom`), `marketplace`, `lang`, `model`, `temperature`, `response_format` и `prompt_text`. Наполняется базовым набором через Seed, включая универсальный промпт-генератор `extract:generator` (создаёт индивидуальный промпт извлечения для каждой категории `subjectID`); сгенерированные промпты категорий сохраняются под кодом `extract:subject:<subjectID>` (`variant = custom`). |
| `wb_products` | «Паспорт» товара — корневые параметры карточки: наименование, бренд, габариты, вес, `vendorCode`, `nmID`, `imtID`, `subjectID`, `chrtID`/`skus`/`techSize`/`wbSize` (из массива `sizes`). Новые скалярные поля из ответа API добавляются в таблицу автоматически. Служебные колонки `is_deleted` (мягкое удаление) и `last_seen` (метка последней сверки). |
| `wb_product_values` | Значения (EAV): характеристики `ART + SUP + charcID → value`, а также универсальные вложенные поля `ART + SUP + field_name → value` (например, `photos` и `documents` хранятся здесь как JSON с полной структурой). |
| `wb_charcs` | Справочник характеристик Wildberries: `charcID`, русское название, признак обязательности, признак наличия именованного поля, ключ JSON-маппинга и связь с категориями (`subj_ID`, `subj_NM`). |
| `oz_products` | «Паспорт» активного товара Ozon: `offer_id`, `name`, `product_id` (из `id`), `sku`, `barcode` (из `barcodes`), `description_category_id`, `type_id`, цены (`price`, `old_price`, `min_price`), `currency_code`, `vat`, флаги (`is_archived`, `is_autoarchived`, `is_discounted`, …), `volume_weight`, габариты (`height`/`depth`/`width`/`weight` + единицы), `created_at`/`updated_at`, `images` (JSON). Неизвестные скалярные поля добавляются в колонки автоматически. Ключ `UNIQUE(ART, SUP)` — артикул выделяется из `offer_id` той же логикой, что и на WB. Служебные колонки `is_deleted`/`last_seen`. |
| `oz_archive` | Архивные карточки Ozon — тот же набор корневых колонок, что и `oz_products` (снимок без деталей атрибутов). Ключ `UNIQUE(ART, SUP)`. Служебные колонки `is_deleted`/`last_seen`. |
| `oz_product_values` | Значения активных карточек Ozon (EAV): характеристики `ART + SUP + attribute_id → value` (из `/v4/product/info/attributes`) и вложенные поля `ART + SUP + field_name → value` (JSON) — например `sources`, `commissions`, `stocks`, `statuses`. |
| `oz_charcs` | Справочник атрибутов Ozon: `attribute_id`, `name`, `description`, `is_required`, `is_collection`, `data_type`, `dictionary_id`. |

**Ключевые технические детали:**

- **Извлечение ART/SUP.** Из строки `vendorCode` регулярными выражениями выделяется ведущая цифровая часть — артикул `ART`, и буквенный остаток поставщика — `SUP`.
- **Инкрементальная выгрузка.** Курсор последней выгрузки сохраняется в `data/wb_cards_cursor.json`, поэтому повторные запуски скачивают только созданные/обновлённые карточки, а не весь каталог заново.
- **Сверка удалённых карточек (soft delete).** Сопоставление карточек с базой идёт по стабильному `nmID` (WB) / `product_id` (Ozon), а не по `ART+SUP`, поэтому смена артикула на площадке обновляет существующую строку, а не создаёт дубликат. При полной выгрузке (когда курсор отсутствует) каждая встреченная карточка получает `last_seen = sync_ts`, а в конце все карточки с `last_seen != sync_ts` помечаются `is_deleted = 1` (данные сохраняются). Вернувшаяся карточка автоматически «воскресает» (`is_deleted = 0`).
- **Уважение к лимитам API.** Пауза `0.6 с` между запросами карточек и `1 с` между опросами категорий, плюс ретраи при `HTTP 429` и сетевых ошибках (до 3 попыток).
- **Колонки `price`/`cost`** в `wb_products` не заполняются самим `DBase` — они предназначены для заполнения другими модулями (например, модулем цен), чтобы не затирать уже загруженные значения.
- **Безопасный fallback курсора.** Если запрос карточек по связке `updatedAt + nmID` возвращает ошибку, модуль стирает `nmID` и повторяет запрос только по `updatedAt` — это обходит случаи, когда Wildberries отклоняет курсор с устаревшим `nmID`.
- **Очистка «хвостов» EAV.** Перед записью динамических характеристик изменённой карточки модуль выполняет `DELETE` старых значений в `wb_product_values` по связке `ART + SUP`, после чего записывает актуальные значения чистым `INSERT`. Это удаляет характеристики, которых больше нет в карточке WB.
- **Саморасширяемая схема.** При получении ответа `DBase` сам распознаёт новые данные: неизвестные скалярные поля верхнего уровня автоматически создают колонки в `wb_products` (с подбором SQL-типа), а вложенные объекты/массивы (`documents` и любые будущие) автоматически пишутся строками в `wb_product_values` (колонка `field_name`, значение — JSON). Это защищает от нововведений со стороны платформы WB без правки кода.
- **Очистка устаревших колонок.** Колонки, выведенные из схемы (например, старая `Photos`), удаляются автоматически через `ALTER TABLE DROP COLUMN` (список `OBSOLETE_PRODUCT_COLUMNS`); колонки, добавленные под новые поля API (например, `video`), при этом сохраняются.
- **Разделение актив/архив Ozon.** Признак архива — поле `is_archived` (bool) в ответе `product/info/list`. Карточка, ушедшая в архив, удаляется из `oz_products` и `oz_product_values` и пишется в `oz_archive`; при восстановлении — наоборот. Это держит связку `ART + SUP` на обеих платформах синхронной.
- **Пагинация Ozon по `last_id`.** Список товаров выкачивается страницами по 100 штук, пока Ozon возвращает непустой `last_id`; детали запрашиваются батчами до 1000 `product_id` за запрос.
- **Авторизация Ozon.** Запросы идут с заголовками `Client-Id` и `Api-Key` (токен — `OZ_MASTER_TOKEN`, Client-Id — `OZON_CLIENT_ID`). Этап Ozon выполняется после WB и пропускается, если ключи не заполнены.
- **Характеристики Ozon.** Значения характеристик товаров приходят не в `product/info/list`, а отдельным методом `/v4/product/info/attributes`; они пишутся в `oz_product_values` по `attribute_id`, габариты (`height`/`depth`/`width`/`weight` и единицы) — в `oz_products`. Вложенные характеристики `complex_attributes` и список `attributes_with_defaults` сохраняются как JSON в `oz_product_values` (по `field_name`).

#### 🔗 `Stack.py` — объединение и разъединение карточек товаров (moveNm)

Модуль `Stack` управляет группами карточек Wildberries через метод Content API `POST /content/v2/cards/moveNm`: объединяет карточки в одну группу (один `imtID`) и разъединяет их, создавая новые группы.

**Что делает модуль при запуске:**

1. **Обновление базы данных.** Первым делом вызывает `apps.DBase.run()`, чтобы актуализировать локальную базу `data/inventory.db` по курсору и подтянуть карточки, созданные продавцом только что.
2. **Поиск по артикулу.** В графическом окне (на `ctk.CTkToplevel`, наследует тему лаунчера) есть поле ввода `vendorCode`. Поиск выполняется напрямую по SQLite-таблице `wb_products` (только активные карточки — с фильтром `is_deleted = 0`). Если введён только числовой `ART`, а в базе есть несколько карточек с одним артикулом, но разными буквенными суффиксами `SUP`, модуль автоматически добавляет **все** найденные `nmID` в рабочий список.
3. **Контроль состава списка.** Все добавляемые карточки должны относиться к одному предмету (`subjectID`); карточки другого предмета пропускаются с предупреждением. Счётчик следит за лимитом Wildberries — не более **30** карточек за одну операцию.

**Операции с группами (через `POST /content/v2/cards/moveNm`):**

| Кнопка | Тело запроса | Результат |
| --- | --- | --- |
| **Объединить** | `{ "targetIMT": <imtID первой карточки>, "nmIDs": [...] }` | Все выбранные `nmID` объединяются в группу с указанным `imtID`. |
| **Разъединить в новую группу** | `{ "nmIDs": [...] }` (без `targetIMT`) | Выбранные карточки получают один общий новый `imtID`. |
| **Разъединить поштучно (Уникализировать)** | по одному `{ "nmIDs": [<nmID>] }` за запрос | Каждая карточка получает уникальный `imtID`. |

**Ключевые технические детали:**

- **Источник данных.** Модуль полностью работает с SQLite-базой `data/inventory.db` (таблица `wb_products`): `vendorCode`, `nmID`, `imtID`, `subjectID`, наименование. Файлы `settings.json` и `cards.json` не используются.
- **Ключ API.** Токен категории `CONTENT` читается из общего файла `.env` через `DBase.get_wb_token("CONTENT")` с fallback на мастер-токен.
- **Соблюдение Rate-limit.** Поштучное разъединение отправляет запросы последовательно с паузой **600 мс** между ними, не нарушая лимиты контент-API (100 запросов/мин).
- **Обработка ошибок.** Все сетевые запросы обёрнуты в обработку `requests.RequestException`; результаты и ошибки показываются через `messagebox`.
- **Потокобезопасность.** Окно создаётся на главном потоке через `root.after(...)`, а сетевые операции выполняются в фоновых потоках — интерфейс не блокируется.
- **Ввод штрихкодов.** Штрихкод можно вставить из буфера обмена сочетанием `Ctrl+V` (или `Ctrl+М` в русской раскладке); после ввода поле автоматически очищается для следующего штрихкода.

#### 🧩 `CardsCreator.py` — пошаговое создание карточек товаров WB и Ozon

Модуль `CardsCreator` — графический мастер создания, редактирования и восстановления карточек товаров на Wildberries и Ozon.

**Что делает модуль сейчас:**

1. **Ввод штрихкода/артикула.** Открывается окно, куда значение можно отсканировать (сканер работает как клавиатура), вставить из буфера обмена (`Ctrl+V` / `Ctrl+М` в любой раскладке или кнопка «Вставить») либо набрать вручную. Поиск запускается по `Enter` или кнопке «Далее».
2. **Поиск по ART.** Из введённого значения выделяется цифровой артикул `ART` (функция `DBase.extract_art_sup`) и ищется в локальной базе `data/inventory.db` по таблицам `wb_products` и `oz_products` — включая удалённые карточки.
3. **Выбор товара.** Если найдено одно или несколько совпадений (одинаковый `ART`, но разные `SUP` и/или площадки WB/Ozon) — открывается список товаров и кнопка «Новый поставщик». Активные карточки открывают режим редактирования (будет реализован на следующем этапе), а удалённые показываются с пометкой **«(Удалено)»**; при выборе такой карточки отображается кнопка **«Восстановить»**.
4. **Восстановление удалённой карточки.** По кнопке «Восстановить» открывается одно окно со всеми данными карточки: артикул/`SUP`, наименование, бренд, описание, габариты и вес, цена/себестоимость (в секции «Основные»), категория и размер, КИЗ-маркировка, документы (галочка «Документы не нужны», включена по умолчанию), характеристики (все обязательные поля категории из справочника `wb_charcs` плюс уже сохранённые значения) и секция выбора фото (без DeepSeek-анализа). Секции сворачиваются стрелками; все поля можно проверить и отредактировать. По кнопке «Далее» карточка ставится в общую фоновую очередь (см. п. 8).
5. **Новый товар (ART не найден): выбор категории.** Если совпадений в базе нет, открывается окно «Выбор категории» со списком всех категорий (предметов) Wildberries из `wb_products` и количеством товаров в каждой. По умолчанию список сортируется по количеству (больше товаров — выше), переключатель позволяет отсортировать по алфавиту. При выборе категории, у которой ещё нет персонального промпта, сначала генерируется промпт через `extract:generator` (подставляются характеристики категории из `wb_charcs` и общие паспортные параметры) и открывается окно редактирования для правки и сохранения; у категорий с уже созданным промптом рядом с кнопкой показывается шестерёнка ⚙️ для ручного редактирования. После этого открывается экран подготовки фото. Черновик карточки создаётся автоматически сразу после сканирования штрихкода и обновляется при каждом действии (ввод `SUP`, выбор категории, выбор и пометка фото, получение характеристик, генерация описания). При повторном сканировании `ART`, для которого остался черновик, программа предлагает восстановить прерванную работу (фото, категория, `SUP` и распознанные характеристики).
6. **Копия карточки с новым SUP.** По кнопке «Новый поставщик» (для уже существующего `ART`) открывается та же форма, что и при восстановлении, но в режиме копии: шаблоном служит первая активная WB-карточка этого `ART`, добавляется поле «SUP (новый)», `vendorCode` формируется как `ART-SUP`, а фото не выбираются вручную — берутся ссылки `big` из БД (`wb_product_values`, поле `photos`) и загружаются одним запросом `POST /content/v3/media/save`. DeepSeek не используется.
7. **Анализ фото через DeepSeek.** Токен задаётся в окне «⚙️ DeepSeek» (хранится в `.env`, ключ `DEEPSEEK_TOKEN`). Кнопка «Анализ DeepSeek» (или клавиша Enter) на экране подготовки фото отправляет отмеченные галочкой «Анализ» фото в модель `deepseek-flash` (`POST /chat/completions`). Системный промт для категории берётся из `promts` (`extract:subject:<subjectID>`); он генерируется и (при необходимости) редактируется на этапе выбора категории. Если промт отсутствует — догенерируется через `extract:generator`, при сбое — fallback на `extract:standard`. Параллельно (вторым запросом по тем же фото) генерируется «Описание» промтом `description:photo` (`response_format = text`, `temperature = 0.5`), который сразу подставляется в поле «Описание»; кнопка ✨ рядом с полем перегенерирует его заново по тем же фото. Ответ парсится как JSON `{"characteristics": [...]}` и открывает окно «Конструктор полей»: показываются все поля категории (паспортные параметры — кроме баркода, который генерируется автоматически, характеристики из `wb_charcs`, а также «Размер (techSize)» по умолчанию `"0"`, «Рос. размер (wbSize)», «Цена» и «Стоимость»; отдельно — галочки «Документы не нужны» и «КИЗ-маркировка») — заполненные и незаполненные, каждое с галочкой. Отмеченные поля сохраняются в `category_fields`; при следующей карточке этой категории показываются только они (скрытые можно вернуть через «Показать все поля»). Для отладки запрос и ответ DeepSeek дополнительно пишутся в файл `data/deepseek_debug.txt` (временно, будет удалён после отладки). Универсальный промпт-генератор `extract:generator` при генерации классифицирует каждую характеристику по способу получения значения (`факт` с фото / `синоним` / `вывод`) и требует помечать выводные значения маркером `source` (`photo` / `inferred`), которые в окне сверки помечаются «вывод». Распознанное значение поля «Бренд» дополнительно проверяется по справочнику `brands` (колонка `search_aliases`, сравнение без учёта регистра): при совпадении в поле подставляется точное имя `wb_name`; если бренд не найден — открывается окно создания нового бренда (юр. название, Бренд ВБ, Бренд ОЗ, Страна), а распознанное значение автоматически добавляется в `search_aliases` (если введённые WB/Ozon уже существуют в справочнике — алиас просто дозаписывается вместо создания дубликата).

**Ключевые технические детали:**

- **БД.** Модуль читает таблицы `wb_products`/`oz_products`/`wb_product_values` и обновляет только строки `price`/`cost` в `wb_products` (данные, не схему); схемой управляет только `apps/DBase.py`, актуальность базы обеспечивает модуль `DBase`.
- **Потокобезопасность.** Окно создаётся на главном потоке лаунчера через `root.after(...)` (тот же паттерн, что и у `Stack.py`); сетевые операции выполняются в одном фоновом потоке-воркере (`queue.Queue`), который обрабатывает задачи по одной и не блокирует интерфейс, а обновление статуса идёт через `window.after(...)`.
- **Хранение.** Исходные фото — в `data/PhotosDrop/`; черновики — в `data/cards_creator_drafts.json` (UTF-8, `indent=2`), по одной записи на карточку (ключ `vendor_code`): `art`, `sup`, категория, список фото (с флагами `main`/`analysis`), характеристики и описание. Черновик создаётся сразу после сканирования и обновляется при каждом изменении карточки, а после успешного создания (см. п. 8) удаляется вместе с исходными фото; пул баркодов — в `data/wb_barcode_pool.json`; отложенные цена/себестоимость новых карточек — в `data/pending_card_costs.json`; токен DeepSeek — в `.env`.

8. **Отправка карточки и фоновая очередь.** По кнопке «Далее» (конструктор новой карточки, форма восстановления или форма копии) генерируется баркод из пула, формируется тело `POST /content/v2/cards/upload`, запрашивается остаток и флаг «Добавить на Ozon», после чего задача ставится в общую фоновую очередь, и программа сразу возвращается к вводу штрихкода. Один поток-воркер (`queue.Queue`) обрабатывает задачи строго по одной — одновременная загрузка двух карточек исключена, интерфейс не блокируется: отправка карточки → опрос `cards/list`/`cards/error/list` до появления карточки без ошибок → загрузка медиа (локальные файлы через `media/file` для новых/восстановленных, ссылки через `media/save` для копий) → проверка числа фото → установка остатка (`PUT /api/v3/stocks/{warehouse}`). Статус очереди отображается постоянной строкой внизу окна ввода. Цена уходит в запрос; цена/себестоимость новых карточек временно сохраняются в `data/pending_card_costs.json` и переносятся в `wb_products`, когда карточка появится в БД. После успешного завершения всех этапов модуль запускает `DBase.run()` для актуализации базы, проверяет, что карточка появилась в `wb_products` по `vendorCode`, и только затем удаляет её черновик и исходные фото из `data/PhotosDrop`.

#### 🔄 Полное обновление базы данных (`_Full_Update.py`)

Служебный модуль `_Full_Update` (имя файла начинается с `_`, поэтому он не показывается отдельной кнопкой в списке модулей) удаляет сохранённый курсор инкрементальной выгрузки и запускает полную выгрузку каталога Wildberries заново. Запускается квадратной кнопкой «🔄» рядом с «D Base» в лаунчере.

**Что делает модуль при запуске:**

1. **Запрос подтверждения.** Через диалоговое окно спрашивает, действительно ли нужно удалить курсор и перескачать все карточки (при запуске без лаунчера — через консоль).
2. **Удаление курсора.** Удаляет `data/wb_cards_cursor.json`, чтобы следующая выгрузка была полной, а не инкрементальной.
3. **Полное обновление БД.** Вызывает `apps.DBase.run()` — все карточки товаров и характеристики скачиваются заново целиком. Отсутствующие в выгрузке карточки помечаются `is_deleted = 1` (soft delete).

**Когда использовать:** после изменения схемы БД, добавления новых полей или когда нужно гарантированно перечитать весь каталог (обычный запуск `DBase` подтягивает только созданные/обновлённые карточки).

**Ключевые технические детали:**

- **Источник данных.** Использует общий путь курсора `DBase.CARDS_CURSOR_PATH` и метод `DBase.run()`.
- **Потокобезопасность.** Диалог подтверждения и сообщение о завершении показываются на главном потоке через `root.after(...)`, а само обновление выполняется в фоновом потоке лаунчера.

#### 🤖 `Bot.py` — Telegram-бот мониторинга заказов и синхронизации остатков

Модуль `Bot` запускает фонового **демона** (`_BotDaemon.py`, имя начинается с `_`, поэтому отдельной кнопкой не показывается), который отслеживает новые заказы Wildberries и Ozon, уведомляет в Telegram и синхронизирует остатки между площадками. Демон работает в отдельном процессе и продолжает работу после закрытия лаунчера; индикатор-кружок справа от кнопки показывает состояние (зелёный — работает, красный — остановлен). Справа от кружка расположена кнопка «🖥», открывающая консоль логов в реальном времени (повторный клик скрывает окно). Клик по кнопке `Bot` переключает демон: при зелёном индикаторе — останавливает, при красном — запускает.

**Что делает бот:**

1. **Wildberries.** Опрашивает `GET /api/v3/orders/new` (интервал `WB_CHECK_INTERVAL` минут). Новые заказы пишутся в таблицу `wb_orders` (активные и история — в одной таблице) с уведомлением в Telegram (💜) и фото товара. В сообщение из базы подставляются артикул, наименование, **тип обложки** и **количество страниц** (характеристики `wb_product_values` по `charcID` 1185 и 90633), цена заказа, а также текущий остаток WB («Остаток: N шт»). При исчезновении заказа из `/orders/new` запрашиваются статусы `POST /api/v3/orders/status`.
2. **Ozon.** Опрашивает `POST /v4/posting/fbs/unfulfilled/list` (статус `awaiting_packaging`). Новые отправления пишутся в `oz_orders` с уведомлением (💙), зеркальным списанием остатка на WB и строкой остатков («Остаток: Oz - Nшт, Wb - Mшт», остаток Ozon читается через `POST /v4/product/info/stocks`).
3. **Синхронизация остатков.** После нового заказа WB остаток запрашивается через `POST /api/v3/stocks/{warehouse}` и — **только если товар есть в активных карточках Ozon** (`oz_products`) — обновляется через `POST /v2/products/stocks`. Этот же запрошенный остаток выводится в уведомлении о новом заказе WB. Это убирает лишние запросы к Ozon для товаров, которых там нет.
4. **Дедупликация уведомлений.** Флаги `notified_new`/`notified_cancel`/`is_active`/`saw_sorted` в `wb_orders` гарантируют, что сообщение не отправится повторно, даже если заказ несколько циклов остаётся в `/orders/new`.
5. **Отмены (отдельная группа).** Каждому сочетанию статусов (`supplierStatus` × `wbStatus`) соответствует уникальное сообщение: `new` → «Отмена нового заказа…», `confirm` → «Отмена собранного заказа…», `complete` → «Отмена отправленного заказа…», плюс отдельные формулировки для `cancel`, `cancel_carrier`, `defect`, `canceled_by_carrier` и т.д. Отмены отправляются в отдельную группу `TELEGRAM_CANCELS_GROUP_ID`. Сверка по `GET /api/v3/orders` (диапазон `dateFrom`/`dateTo`) с дозапросом статусов через `POST /api/v3/orders/status` ловит отмены, случившиеся пока бот был выключен.
6. **Кнопка «Заказать» в личном чате.** Уведомление о новом заказе WB дополнительно дублируется в личный чат владельца (`YOUR_TELEGRAM_ID`) с inline-кнопкой «Заказать»; в группу заказов уведомление уходит без кнопки. При нажатии кнопки бот отправляет в группу `TELEGRAM_ORDER_BUTTON_GROUP_ID` сообщение вида «Арт. {артикул}\n{название}» (фото + подпись через `sendPhoto`, либо текст через `sendMessage`) от имени бота.

**Ключевые технические детали:**

- **Хранение.** Все данные заказов — в SQLite `data/inventory.db` (таблицы `wb_orders`, `oz_orders`); JSON-файлы бота не используются. Неизвестные скалярные поля ответа API автоматически создают колонки (механизм `DBase.ensure_order_columns`), вложенные структуры хранятся как JSON.
- **Обновление каталога.** Если в заказе встречается неизвестный артикул, вызывается `DBase.refresh_wb_cards()` — инкрементальное обновление каталога по курсору, без полной перевыгрузки.
- **Копируемый артикул.** В уведомлениях (новый заказ WB, отмена, «Закончился на ВБ») артикул выводится как нажимаемый фрагмент: копируется только цифровая часть до знака «-» (из «9789853301182-АК» — «9789853301182»), а буквенный суффикс остаётся обычным текстом рядом.
- **Защита от блокировки (анти-бан).** Клиент `ApiClient` открывает circuit breaker при устойчивых ошибках 4xx/5xx (запрос с 4xx учитывается WB как 10 запросов) и уведомляет владельца; ретраи — только на `429` и сетевые ошибки.
- **Одиночный экземпляр.** Lock-файл `data/bot_daemon.lock` не даёт запустить второго демона.
- **Управление.** Кнопка `Bot` в лаунчере переключает демон (запуск/остановка). Команды в Telegram: `/status` — состояние счётчиков, `/stop` — остановить демона (только для `YOUR_TELEGRAM_ID`).
- **Логирование.** Демон пишет логи в `data/logs/bot.log` — по одному файлу на час (`TimedRotatingFileHandler`), хранится последние сутки (24 файла). Папка `data/` исключена из Git, поэтому логи не попадают в репозиторий. Просмотр — в реальном времени через кнопку «🖥» рядом с модулем `Bot`.

---

## 📂 Архитектура и структура папок

```
VERM/
├── main.py                    # Главный лаунчер (GUI на CustomTkinter)
├── requirements.txt           # Список зависимостей проекта
├── PUSK.bat                   # Ярлык запуска системы (pythonw.exe main.py)
├── .env                       # Токены WB (мастер + 7 категорий); создаётся автоматически
├── .gitignore                 # Правила игнорирования Git
├── .clinerules                # Правила архитектуры для AI-агентов
├── apps/                      # Каталог модулей (скриптов-функций)
│   ├── __init__.py            # Маркер пакета apps
│   ├── DBase.py               # Модуль: схема БД + выгрузка данных из WB API
│   ├── _Full_Update.py        # Служебный модуль: полное обновление БД (сброс курсора)
│   ├── Bot.py                 # Модуль: запуск Telegram-бота (демон)
│   ├── CardsCreator.py        # Модуль: пошаговое создание карточек WB/Ozon
│   ├── _BotDaemon.py          # Служебный демон бота (процесс, не показывается в списке)
│   └── Stack.py               # Модуль: объединение/разъединение карточек (moveNm)
└── data/                      # Единое хранилище данных
    ├── config.json            # Конфиг лаунчера (тема, цвет, размер окна, автозапуск DBase, интервал таймера)
    ├── inventory.db           # SQLite-база данных
    ├── wb_cards_cursor.json   # Курсор инкрементальной выгрузки карточек
    ├── PhotosDrop/            # Исходные фото новых товаров (CardsCreator)
    ├── cards_creator_drafts.json  # Черновики создаваемых карточек (CardsCreator)
    └── logs/                  # Логи бота (bot.log + почасовая ротация, хранится сутки)
```

### Назначение каталогов и ключевых файлов

| Путь | Назначение |
| --- | --- |
| `main.py` | Точка входа приложения. Сканирует `apps/`, строит GUI и запускает модули в фоновых потоках. Менять не требуется. |
| `apps/` | Домашний каталог всех модулей. Лаунчер автоматически обнаруживает здесь новые `*.py`-файлы. |
| `apps/DBase.py` | Управляет схемой БД и интегрируется с Wildberries Content API v2 (выгрузка карточек и характеристик). |
| `apps/CardsCreator.py` | Мастер пошагового создания/редактирования/восстановления карточек WB/Ozon: ввод артикула, поиск по `ART`, выбор категории для новых товаров, подготовка фото (`data/PhotosDrop`), анализ фото через DeepSeek со сверкой характеристик и подстановкой точного имени бренда из справочника `brands`, а также создание новых карточек, копий с новым `SUP` и восстановление удалённых по общему фоновому конвейеру (баркоды, `cards/upload`, загрузка фото файлом/по ссылкам, остатки). |
| `apps/_Full_Update.py` | Служебный модуль полного обновления БД (запускается кнопкой «🔄» в лаунчере): удаляет курсор и запускает полную выгрузку каталога. |
| `apps/Stack.py` | Модуль объединения/разъединения карточек (nmID) через метод `moveNm`; читает данные из `data/inventory.db`. |
| `apps/__init__.py` | Делает `apps/` пакетом Python; модули импортируются как `apps.<имя>`. |
| `data/` | Единое хранилище всех данных и служебных файлов, генерируемых модулями. |
| `data/config.json` | Настройки лаунчера: `appearance_mode`, `color_theme`, `window_size`, `dbase_auto_start`, `dbase_interval_minutes`. |
| `data/inventory.db` | SQLite-база данных проекта (таблицы `brands`, `status`, `wb_products`, `wb_product_values`, `wb_charcs`). |
| `data/wb_cards_cursor.json` | Сохранённый курсор последней выгрузки карточек для инкрементального обновления. |
| `data/logs/` | Логи бота: `bot.log` с почасовой ротацией (`TimedRotatingFileHandler`), хранятся последние сутки (24 файла). Не попадает в Git (игнорируется через `data/*`). |
| `data/PhotosDrop/` | Исходные фото новых товаров (модуль `CardsCreator`) — плоский список для быстрого выбора. |
| `data/cards_creator_drafts.json` | Черновики создаваемых карточек (модуль `CardsCreator`, UTF-8, `indent=2`). |
| `data/wb_barcode_pool.json` | Пул сгенерированных баркодов WB для восстановления карточек (модуль `CardsCreator`, UTF-8, `indent=2`). |
| `requirements.txt` | Зависимости: `customtkinter==6.0.0` (GUI), `requests==2.32.3` (HTTP-клиент), `aiohttp==3.14.3`, `Pillow==12.3.0` (миниатюры предпросмотра фото). |
| `.env` | Токены Wildberries: `WB_MASTER_TOKEN` и 7 специализированных (`WB_TOKEN_CONTENT`, `WB_TOKEN_PRICES`, `WB_TOKEN_STOCKS`, `WB_TOKEN_MARKETPLACE`, `WB_TOKEN_STATISTICS`, `WB_TOKEN_PROMOTION`, `WB_TOKEN_FEEDBACKS`). Не хранится в Git. |
| `PUSK.bat` | Запускает приложение через `pythonw.exe` (без консольного окна). |
| `.gitignore` | Исключает из Git `.venv/`, `__pycache__/` и `data/*` (кроме `config.json`). |

> **Важно:** каталоги `apps/` и `data/` создаются лаунчером автоматически при первом запуске, поэтому проект готов к работе «из коробки».

---

## 🛠️ Инструкция по установке и запуску

### Требования

- **Python 3.14** (проект протестирован на Python 3.14.0) или более новая совместимая версия;
- доступ к интернету для установки зависимостей и работы с API Wildberries;
- действующий **токен Wildberries** (мастер-токен `WB_MASTER_TOKEN` либо специализированные токены).

### Пошаговый гайд (Windows)

1. **Склонируйте/скопируйте проект** в нужную папку и перейдите в неё:

   ```powershell
   cd C:\путь\к\проекту\VERM
   ```

2. **Создайте виртуальное окружение:**

   ```powershell
   py -m venv .venv
   ```

3. **Активируйте виртуальное окружение:**

   ```powershell
   .venv\Scripts\activate
   ```

4. **Установите зависимости:**

   ```powershell
   python -m pip install -r requirements.txt
   ```

5. **Укажите токены и параметры API.** При первом запуске модуля `DBase` файл `.env` создастся автоматически, либо создайте его вручную в корне проекта. Для Wildberries поддерживается 1 мастер-токен (резервный) и 7 специализированных токенов по категориям методов; для Ozon — мастер-токен, массивы ID складов и Client-Id:

   ```dotenv
   # Секретные ключи Wildberries и Ozon.
   WB_MASTER_TOKEN="ВАШ_ТОКЕН_ЗДЕСЬ"
   WB_TOKEN_CONTENT=""
   WB_TOKEN_PRICES=""
   WB_TOKEN_STOCKS=""
   WB_TOKEN_MARKETPLACE=""
   WB_TOKEN_STATISTICS=""
   WB_TOKEN_PROMOTION=""
   WB_TOKEN_FEEDBACKS=""
   OZ_MASTER_TOKEN=""
   WB_WAREHOUSE_ID=""
   OZON_WAREHOUSE_ID=""
   OZON_CLIENT_ID=""
   ```

   Замените `ВАШ_ТОКЕН_ЗДЕСЬ` на реальный мастер-токен (его можно получить в личном кабинете Wildberries → раздел «Контент» → API). Специализированные токены WB можно оставить пустыми — тогда для них автоматически будет использоваться `WB_MASTER_TOKEN`. `WB_WAREHOUSE_ID` и `OZON_WAREHOUSE_ID` задаются в виде JSON-массива (например `"[37356, 12345]"`) или перечислением через запятую. Все значения также можно заполнить прямо в окне настроек лаунчера (кнопка «⚙️»).

6. **Запустите приложение** одним из способов:

   ```powershell
   python main.py
   ```

   или через ярлык:

   ```powershell
   PUSK.bat
   ```

   В окне лаунчера появится кнопка **«D Base»** — нажмите её, чтобы инициализировать базу данных и выгрузить карточки товаров.

> **Примечание.** `PUSK.bat` запускает приложение через `pythonw.exe`, поэтому окно консоли не показывается. Если нужны логи в консоли — используйте `python main.py`.

---

## ➕ Инструкция по добавлению новых модулей

Чтобы лаунчер корректно обнаружил и запустил новый скрипт, он должен лежать в папке `apps/` и соответствовать простым правилам:

1. Имя файла — `*.py`, **не** `__init__.py` и **не** начинается с символа `_`.
2. В модуле обязательно должна быть объявлена вызываемая функция **`run()`** — именно её лаунчер вызывает в фоновом потоке.
3. Модуль выполняется в отдельном потоке и **не должен** напрямую обращаться к GUI (Tkinter/CustomTkinter не потокобезопасны).
4. Все выходные файлы, логи и кэши модуль должен сохранять **только** в каталог `data/`.
5. Изменять схему базы данных (создавать/менять таблицы и колонки) может **только** `apps/DBase.py`; остальные модули работают только со строками данных.
6. Если модулю нужна новая библиотека — сначала добавьте её в `requirements.txt` и установите через `.venv\Scripts\pip.exe install -r requirements.txt`.

### Минимальный шаблон модуля

```python
import time
import logging


def run() -> None:
    """
    Главная точка входа модуля.
    Вызывается лаунчером в отдельном фоновом потоке.
    """
    print("[Мой модуль] Started execution...")

    # Здесь — логика вашего модуля
    # (выгрузка данных, обработка, запись в data/ и т.д.)

    print("[Мой модуль] Finished successfully.")


if __name__ == "__main__":
    # Позволяет запускать модуль и напрямую: python apps/имя_модуля.py
    run()
```

### Пошаговый пример

1. Создайте файл `apps/MyModule.py` с кодом выше.
2. Замените логику внутри `run()` на нужную.
3. В лаунчере нажмите кнопку **«Обновить список»** (или перезапустите приложение).
4. Появится кнопка **«My Module»** — нажмите её для запуска.

Готово! Лаунчер автоматически импортирует модуль как `apps.MyModule`, вызовет его функцию `run()` в отдельном потоке и покажет статус выполнения в нижней строке окна.

